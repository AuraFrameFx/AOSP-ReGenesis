All modules must use the pinned toolchains and versions. Do not change Java from 24, Kotlin from 2.2.x, or Gradle plugin versions outside the version catalog without a documented RFC (build/ADR-xxx) and approval.

Notes:
- Kotlin DSL is required across the repo.
- Version drift is CI-blocking.
